# Pinned dependencies for the ENTOD Division Sales Summary dashboard.
# Run `python requirements.py` to install, or `pip install -r requirements.txt`.
streamlit>=1.32.0
pandas>=2.0.0
openpyxl>=3.1.0
plotly>=5.18.0
pyxlsb>=1.0.9
